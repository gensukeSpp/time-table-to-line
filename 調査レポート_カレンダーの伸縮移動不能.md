# 調査レポート: タイムテーブル(Calendar)のイベント伸縮・移動ができない

調査日: 2026-08-20(実機確認: ブラウザ/Playwright 相当、ユーザー 20 でログインして再現)
ブランチ: feature/milestone-start

## 1. 確認した症状(実機)

- カレンダーの週表示でイベント(「IFTTT にやられる」11:00 AM – 12:00 PM)を下方向へドラッグ。
- **ドラッグ操作は発火する**(ドロップ後に「変更する」「リセット」「変更回数: 1」ボタンが出現 → `onEventDrop` は呼ばれている)。
- しかし**イベントは元の位置(11:00 AM – 12:00 PM)に留まったまま動かない**。
- さらに同一イベントが **2 つ表示される(重複描画)**。

→ 「ドラッグは開始できるが、新しい日時が画面に反映されない」状態。

## 2. 根本原因 (2 系統)

### 原因 1: フロント — アクセサとドロップ処理のフィールド不一致

カレンダーの**描画位置**は `startAccessor` / `endAccessor` が返す値で決まる。

- `src/components/pages/CalendarView.tsx:137-142`
  ```ts
  startAccessor={(stateEvent) => stateEvent.start_time}
  endAccessor={(stateEvent) => stateEvent.end_time}
  ```

一方、ドロップ時に新しい日時を書き込む `onEventDrop` / `onEventResize` は、`start`/`end` フィールドへ書く。

- `src/hooks/useMouseHandle.ts:29-42`(onEventDrop)、同 12-27(onEventResize)
  ```ts
  const movedEvent: TimelineEventProps = {
    ...handleEvent,          // start_time / end_time は元のまま
    start: new Date(start),  // ← 新しい日時はここに入るだけで…
    end: new Date(end),
    isDraggable: true
  }
  ```

→ `start_time`/`end_time` が更新されないため、アクセサ経由の再描画は**常に元位置**。これが移動・伸縮不能の直接原因。

### 原因 2: フロント — 元イベント削除の prevRef ロジックが効かず、二重描画

`CalendarView.tsx:70-75` が、ドロップ時の元イベントを `state` から削除しようとする。

```ts
state?.map((evt, j) => {
  if (prevRef.current?.isDraggable === true && prevRef.current.id === evt.id) {
    delete state[j];          // state 配列を破壊的 delete(ただし state は props/Context 由来)
    prevRef.current = undefined;
  }
});
```

- `state` は `useEventsState()` で得た配列。`prevRef` は `useMouseHandle` 内の `useRef`。
- ドロップで `eventList`(=変更後イベント)が追加され、元イベントも残る → 二重表示。
- `delete state[j]` は元配列の要素を「穴」にするだけで React の再レンダーで除去される保証もなく、元イベントと新しいイベントが両方描画される。

### 原因 3: バックエンド — 日時保存エンドポイントが存在しない

フロントの保存処理は `POST /date/update` を呼ぶ。

- `src/hooks/useEventMutation.ts:50-70`(`useUpdateDateListMutation`)
  ```ts
  mutationFn: (timeBeltArray) => basicAxios.post(`/date/update`, { data: timeBeltArray })
  ```

しかしバックエンド `light_token_server/app/routers/timetable.py` のルート一覧:

```
GET/POST /timetable/auth
GET/POST /refresh
GET/POST /timetable/inquiry
GET/POST /group/all
GET/POST /group/users
GET/POST /event/all
GET/POST /event/user
GET/POST /group-names
POST /event/add
POST /event/update/{event_id}
DELETE /event/remove/{event_id}
```

→ **`/date/update` が存在しない**(git 全履歴 `git log --all -S "date/update"` でも一度も存在しない)。保存しようとすると 404。

さらに `POST /event/update/{event_id}`(`timetable.py:147-162`)も、`summary` と `progress` しか更新しない:

```py
if body.summary is not None:
    target.summary = body.summary
if body.progress is not None:
    target.progress = body.progress
```

→ `start_time`/`end_time` を更新する経路がバックエンドに**存在しない**。

## 3. データモデルの整合性(正常に機能している部分)

- DB: `app/models.py:99-100` はカラム `start_time` / `end_time`(DateTime)。
- serialize(`models.py:115-116`)で `start` / `end` キーに変換。
- フロント: `src/resources/queries.ts` の `useEventsQuery` が `item.start`/`item.end` を `start_time`/`end_time` へ逆変換してから、カレンダー(`CalendarView`)にもタイムライン(`TimelinePage`)にも渡す。
- `src/lib/TimelineType.ts:15-21` の `TimelineEventProps` は `start_time`/`end_time` を正とし、`start`/`end` も持つ(併存)。

→ つまり「読み込み」パスは整合。「ドロップで書き込む」パスだけが `start`/`end` に寄っていて不整合。

## 4. 回帰の切り分け

- `CalendarView.tsx` と `useMouseHandle.ts` は `origin/release`(リリース 04)と **差分なし**。
- すなわちこの不整合はリリース 04 時点から潜在する構造問題であり、今回のブランチ(マイルストーン関連のリネーム: `MyHorizonTimeline`→`GroupHorizonTimeline` 等)で新たに壊れたわけではない。

## 5. 修正方針(案)

- **フロント(描画)**: `useMouseHandle.ts` の `onEventDrop`/`onEventResize` で、新しい日時を `start_time`/`end_time` にも反映する。ついで `start`/`end` も揃える。
  ```ts
  const movedEvent = {
    ...handleEvent,
    start_time: new Date(start),
    end_time: new Date(end),
    start: new Date(start),
    end: new Date(end),
    isDraggable: true
  }
  ```
- **フロント(二重描画)**: 元イベント除去を `prevRef`/`delete state[j]` に頼らず、`newState` 構築時に id で重複排除する等へ整理。
- **バックエンド(保存)**: `/date/update`(一括日時更新)を実装するか、`/event/update/{id}` を `start_time`/`end_time` 対応にする。あわせてスキーマ/モデルへ `milestone_id`・`completed`(requirement-03)追加が想定される現状と整合させる。

## 6. 補足メモ

- 実機は専用リモートデバッグ Chrome(`--remote-debugging-port=9333`)で確認。本番ユーザーの既定 Chrome はリモートデバッグ未許可でハーネスが直接接続できず、専用インスタンスで代用した。
- テスト(`src/tests/Calendar.spec.tsx`)は `useMouseEvents` を `vi.mock` しており、実 DnD の往復(ドロップ→描画反映)を検証していない。回帰防止には、`useMouseHandle` の出力する `start_time`/`end_time` がアクセサに読まれる形をアサートするテスト追加が有効。
