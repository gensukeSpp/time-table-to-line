---
created: 2026-09-11T10:10
updated: 2026-09-11T10:24
---


task-11（フロント）の実装が完了しました。コミット 1da4b2d / 79cc3b6。

実装内容

1. src/lib/env.ts（新規） — `MILESTONE_REFRESH_INTERVAL_MS`（再取得間隔）と `MILESTONE_CLOSE_GRACE_DAYS`（猶予日数）の 2 定数。`import.meta.env` の `VITE_MILESTONE_REFRESH_INTERVAL_MS` / `VITE_MILESTONE_CLOSE_GRACE_DAYS` から読み、未設定時は各々 60 分（3,600,000ms）/ 5 日にフォールバック。`parseEnvPositiveInt()` で不正値（空文字・非数値・0・負数）も既定値へフォールバック（PR #26 レビュー [P2] 対応）。
2. src/resources/queries.ts:118 — useMilestonesQuery に refetchInterval: MILESTONE_REFRESH_INTERVAL_MS を追加（理由コメント付き）。 他クエリには触っていません。
3. src/resources/queries.spec.tsx — useMilestonesQuery の定期再取得テストを追加。fake timers で「初期フェッチ 1 回 → 間隔途中では増 えない → 間隔経過で 2 回目」を検証。
4. .hermes/rules/TASKS.md 11b 行 — フロント対応を実装済みとして更新。

確認結果（すべて実行済み）

- bun run testrun → 15 ファイル / 81 tests passed（1 skipped は既存）。**PR #26 レビュー指摘の猶予日数テスト不整合を解消**（env 定数から期待値を導出）。[P1]/[P2] 対応後に再実行して通過を確認。
- bun run lint → 警告 0 で通過
- bun run build → tsc + vite ビルド成功

補足

- .env には既に VITE_MILESTONE_REFRESH_INTERVAL_MS=3600000 が設定済み。**PR #26 レビュー [P2] 対応で `.env.example` を新規追加**（`VITE_MILESTONE_REFRESH_INTERVAL_MS` / `VITE_MILESTONE_CLOSE_GRACE_DAYS` の 2 変数を記載）。
- `MILESTONE_CLOSE_GRACE_DAYS` は 5 日が正式採用（2026-09-08 決定）で、**本対応で写経ではなく `.env`(`VITE_MILESTONE_CLOSE_GRACE_DAYS`) から読む仕様に変更**。`src/lib/milestone.ts` の矛盾した TODO コメント（「仮の 2 日」と「5 日後に決定」）も整理。
- ~~timeline_diff.txt（未追跡）と tasks/task-11/overview.md 等の既存変更は task-11 実装と無関係のためコミットしていません。~~

完了後、プランどおり実ブラウザでの確認（猶予間隔を短くして closed 化されたマイルストーンが一覧から消え色がデフォルトに戻ること）は利用者側検証となります。

