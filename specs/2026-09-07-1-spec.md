---
created: 2026-09-07T10:26
updated: 2026-09-07T10:53
---


Issue #23 の並列実装が完了しました。

確認済みの状態

バックエンド（light_token_server） — ご指定どおり、milestone_id の判定を is not None から model_fields_set に変更する対応が app/routers/timetable.py:201-205 で完了していました。null 送信時は明示的に milestone_id=None（所属なし）として保存されます。

フロントエンド（time-table-to-line） — tasks/issue-23/ に沿って全タスクを実装しました。Task 1〜2（純粋ヘルパ＋単体テスト）は親が先に完了させ、Task 3（Timeline 配色）と Task 4〜6（InputItem セレクト／テスト）は互いにファイルが重ならないので 2 つのサブエージェントへ並列で投げ、最後に親が品質ゲートを締めました。

変更内容

ファイル: src/lib/milestoneLookup.ts（新規）
内容: buildMilestoneColorMap / buildMilestoneStatusMap / computeItemDecorations（純粋関数）
────────────────────────────────────────
ファイル: src/tests/milestoneLookup.spec.ts（新規）
内容: open / waiting / 未所属 / 未収録 の 5 ケース
────────────────────────────────────────
ファイル: src/components/pages/TimelinePage.tsx
内容: useMilestonesQuery() ＋ itemRenderer を配線。milestone_id → 所属マイルストーンの色、waiting 時は
  opacity:0.7（未所属はデフォルト色 #2196f3 のまま）。{...}: any は避け、inline 型注釈（ItemContext の Pick 等）で no-explicit-any リンティングを回避。
────────────────────────────────────────
ファイル: src/components/organisms/InputItem.tsx
内容: 編集モードに「マイルストーン：」NativeSelect 追加（open のみ列挙 ＋ 「所属なし」→ null）、読取専用モードに所属名
  or「所属なし」を表示、open 所属の round-trip 担保。
────────────────────────────────────────
ファイル: src/tests/InputItem.spec.tsx
内容: テスト 3 件追加（open 列挙・選択値反映・readOnly 名表示）。既存 readonly テストは green のまま。

品質ゲート結果


bun run build    → tsc + vite build, 0 errors（3.91s）
bun run lint     → eslint --max-warnings 0, 0 警告
bun run testrun  → 14 files, 72 passed / 1 skipped


（skipped 1 件は既存の Calendar.spec.tsx 由来で、本 Issue とは無関係。）

補足

- milestoneLookup.spec.ts は当初、内部不整合なアサーション（closed の id 3 をフィクスチャに入れていたため「map に無い」前提が成立し ない）があり、map に存在しない id 99 に修正しました。
- null（所属なし）保存の DB 保存確認 は ~~バックエンド対応後の手順なので、~~ 実ブラウザで ~~確認してください~~ 確認済みです（test-plan.md §3 の手順 3・5・6 が対象）。
