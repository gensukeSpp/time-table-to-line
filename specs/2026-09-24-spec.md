---
created: 2026-09-24T15:34
updated: 2026-09-24T15:38
---


実装が完了しました。全 5 タスクを TDD で進め、品質ゲートはすべて緑です。

実装内容（ブランチ feature/progress-color/35）

4 commits:

63a2136 docs(issue-35): record implementation and plan in TASKS.md
49b8324 refactor(issue-35): drop static rbc-event-allday color (inline eventPropGetter only)
7ac58da feat(issue-35): wire eventPropGetter for week progress coloring
4930033 feat(issue-35): add progressToColor / resolveEventColor pure functions


変更ファイル
- 新規 src/lib/progressColor.ts — progressToColor（進捗値→色、日本語ラベル/英字トークンの両方を正規化、未知・null は #3174ad へフォ ールバック）+ isMonthAllday（フルデイ||日跨ぎ、Issue #30 の月ビュー描画を回帰ゼロで再現）+ resolveEventColor（view 対応）
- 変更 src/components/pages/CalendarView.tsx — eventPropGetter（useCallback([currentView])、TDZ 回避で currentView 宣言後に配置）を 配線
- 変更 src/components/pages/CalendarView.css.ts — .rbc-event-allday 固定色 #00695c を削除（EW アンカー規則は残存）
- テスト新規 src/tests/progressColor.spec.ts(11) / 追記 src/tests/CalendarView.spec.tsx(→8)

品質ゲート: lint 0 エラー / build 成功 / testrun 19 ファイル 124 passed・1 skipped（既存）— すべて緑。

4 色の確認（次のステップ）

候補色で実装済みです。お手元で実ブラウザ確認をお願いします:


bun run dev   # 週ビューのイベントを進捗別に配色で確認


確認ポイント
1. 週ビューで進捗なしフルデイが #3174ad（デフォルトに戻る）
2. 進捗 4 段階がそれぞれの色: これから #3949ab / まだ #5e35b1 / もうすぐ #8e24aa / 完了 #d81b60
3. 月ビューは従来どおり #3174ad / #00695c（進捗配色が出ない）
4. 月で追加したイベントも週では進捗配色になる
5. イベント編集 → 更新で色が即時反映される

もし 1 色でも調整したくなったら、その進捗の色だけ言っていただければ PROGRESS_COLORS とテスト期待値をまとめて差し替え、再ゲートします。確認が通ったら gh issue close 35 を実行します（ご指示があればこちらで行います）。

実ブラウザ検証済み。配色も承認、変更なし。
