---
created: 2026-07-17T11:54
updated: 2026-07-23T15:43
---
## 概要
かつての [タイムテーブル & タイムライン](https://github.com/Manabu-Aihara/time-table4)アプリのリファクタリング -> 機能追加
リファクタリングは Step by Step で進めていきましょう。

## 目的
ご覧の通り、煩雑で無駄や問題の多い~~汚い~~コードです。
後の機能追加のため、まずは **リファクタリングを最優先** にします。

## リファクタリングを行うの前に
- ライブラリは、整合性のある限り、現時点(2026-07-23)で **最新** のものにします。
	└ vite, react-big-calendar, react-calendar-timeline, tanstack query …
- パッケージ管理ツール yarn -> **bun に移行** します。
- UI ライブラリを統一します。
	└ 現在 chakra, radix-ui はほぼ同機能で、どちらかに一本化、また別の選択肢があれば提案してください。
- コンポーネントテストを行いたいため、Storybook は入れておきたいのですが、Playwright で包括できるなら、その限りではありません。 
- コンポーネントテストは、 vitest 及び Jest では **行いません** 。
- 認証機能は、今のところそのまま(別認証サイトから、トークン認証)にします。

## リファクタリング
**前提**: タイムテーブル(react-big-calendar)、タイムライン(react-calendar-timeline)に表示・追加する予定は、今後 **イベント** と表記します。当アプリの中心的なところです。-> `src/lib/TimelineTypes.ts` の `TimelineEventProps`

**主な構造**
- `src/components/` 以下の4ディレクトリでは、コンポーネント `tsx` の集まりです。古いタイプのフォルダ分けかもしれません(... 知らんけど)。
- `src/lib` 主に型の集まり。
- `src/hooks` フック の集まり。
- `src/resources` fetch 動作、Tanstack Query の動作(キャッシュ、クエリー)。

### 手順と要求
コンポーネント単位で、なるべく早く動作確認が行えることを希望しますが、**細かくタスクを分けて、順序立てて、ゆっくり確実に** 進めていきます。

【重要】リファクタリングにあたって、 **パフォーマンスの低下** や **余分な負荷** になる点も、注意深く探索にあたってください。

- **`src/components/` 以下について** 、フォルダ構造を変えるつもりはありませんが、「このコンポーネントは、普通ここじゃない」意見は **受け入れます** 。
- **提案を要求** :`src/components/` 以下のリファクタリングの手順
	└ `molecules` -> `templates` ? or `templates` -> `molecules` ?
- `lib` `hooks` `resources` についても未使用、不要を省いていきます。
- セキュリティリスクになる `console.log` や、コメントアウトされたかつての~~未練のある~~コード、不要なコメントも削除します。

## 現状の動作不良・問題点
- タイムテーブル: `defaultView="week"` 主に "週" ビューでイベントを操作
	1. PM 11:00 にイベントが追加できない
		└ 11:00 PM に追加されず、イベントが "1日いっぱい(`allDayAccessor` プロパティ)" の箇所に追加されてしまう -> `class="rbc-day-bg"`
	2. 一方 "1日いっぱい" を期待する箇所では、 12:00 AM に追加される(これは、機能追加の要件になるかもしれないので、1 が解決できれば良い)
	3. データベース上のテーブルに、保存されるイベント(`TimelineEventProps` の `start_time` `end_time`)の時刻が、日本時間ではない -> UI では、期待する時刻に挿入される(PM 11:00 を除く)

- タイムライン: タイムテーブルと連動し、タイムテーブルで追加したイベントが表示できる
	1. イベントの「重なり」が表示できない

### ついでのお願い
- こちらは **リファクタリングの過程** で(例: 11:00 PM の問題について、 Calendar コンポーネントの最中)、もしくは、 **別の機会** に問題提起して行っても良いです。
- 下記の「参考リンク」を参照しながら、これら問題点にあたってください。

## 参考リンク
- [react-big-calendar](https://github.com/bigcalendar/react-big-calendar)
- [react-calendar-timeline](https://github.com/namespace-ee/react-calendar-timeline)

---
予告: `requirement-02.md` では、既存ライブラリで使いこなせなかった箇所(`allDayAccessor` プロパティなど)を利用した機能を目的とします。